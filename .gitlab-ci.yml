stages:
  - pyStage
  - monstage

job-1:
  stage: monstage
  image: bash:latest
  tags:
    - qgistest
  script:
    - ./test.sh

job-linter:
  stage: pyStage
  image: bash:latest
  tags:
    - qgistest
  script:
    - python3 -m pylint TestPy.py
